let testUrl = `https://example.com`;

// Argument positions.
let argPositionOptions = 0;
let argPositionUrl = 1;
let argPositionEdgeCheckUrl = 0;

module.exports = {
  testUrl,
  argPositionOptions,
  argPositionUrl,
  argPositionEdgeCheckUrl,
};
